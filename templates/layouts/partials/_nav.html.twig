<nav class="navbar navbar-expand-sm bg-dark navbar-dark fixed-top">
    <div class="container">
        <a href="{{ path('app_homepage') }}" class="navbar-brand mb-3"><img src="{{ asset('images/shop.png') }}" alt=""></a>
        <button class="navbar-toggler" data-toggle="collapse" data-target="#navbarCollapse">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a href="{{ path('app_homepage') }}" class="nav-link">Accueil</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('products') }}" class="nav-link">Produits</a>
                </li>
                {% if app.user %}
                    <li class="nav-item">
                        <a href="{{ path('app_logout') }}" class="nav-link">Deconnexion</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('new') }}" class="nav-link">Admin</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a href="{{ path('app_login') }}" class="nav-link">Connexion</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('inscription') }}" class="nav-link">Inscription</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>